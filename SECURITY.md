# Security Policy

## 范围

`video-to-word-notes` 处理用户指定的视频、音频、字幕和结构化笔记，并调用本地或用户授权的外部工具。主要安全边界包括：

- 使用 ffmpeg 或 `imageio-ffmpeg` 解析不可信媒体；
- 使用 `python-docx` 生成 Word 文件；
- 可选的视频下载、字幕获取和语音转写工具；
- 来源 URL 中可能包含的 Token、签名和隐私参数；
- 生成文件的保存位置与覆盖行为。

项目不会提供绕过登录、验证码、DRM、付费墙或平台访问控制的能力。

## 支持范围

当前维护以 `main` 分支的最新版本为准。报告问题前，请确认问题在最新提交中仍能复现。

## 私下报告漏洞

请不要为安全问题创建公开 Issue。优先使用 GitHub 的私有漏洞报告功能：

1. 打开仓库的 **Security** 页面。
2. 选择 **Report a vulnerability**。
3. 提供受影响的提交、最小复现步骤、实际影响和建议修复方式。

报告中不要附带真实密码、Cookie、Token、签名 URL、私人视频或未经授权的完整转写。需要样本时，请使用人工构造或已脱敏的最小文件。

## 用户安全建议

- 只处理你信任并有权使用的媒体来源。
- 及时更新 ffmpeg、Python、`python-docx`、下载器和转写依赖。
- 对不可信媒体优先在低权限、隔离的环境中处理。
- 不在命令行、日志、Issue 或生成文档中暴露 Cookie、Token 和签名参数。
- 输出已存在时不要使用 `--force`，除非你确认目标文件可以覆盖。
- 使用云端转写或模型服务前，先确认其隐私政策和数据保留设置。
- 发布笔记或 Skill 前，检查其中是否包含私人谈话、个人信息或受版权保护的长篇原文。

## 不属于安全漏洞的内容

一般的转写错误、摘要质量问题、排版建议或不涉及安全影响的依赖安装失败，请通过普通 Issue 报告。
